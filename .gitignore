__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/
.venv/
venv/
.DS_Store

# Live downloads are not committed: they are large, they change, and the frozen
# synthetic dataset is what makes the repository reproducible without network access.
data/raw/live/

# Derived intermediates. Regenerated by run_pipeline.py in seconds; features.csv alone is
# 7MB. The provenance and run-summary JSON files are small and are committed, because they
# document which source produced the results in outputs/.
data/processed/*.csv
